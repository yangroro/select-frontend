{% macro ridi_select_load(
  loginUserId,
  isPc,
  isTablet,
  isMobile,
  isPaper,
  paperDeviceModel,
  isRidiApp,
  freePromotionMonths,
  OAUTH2_CLIENT_ID
) %}
<script>
  window.dispatchEvent(new CustomEvent('ridiSelectLoad'));
</script>
{% endmacro %}
